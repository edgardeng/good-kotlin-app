<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edgar.movie">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DemoApp">
        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/title_activity_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.setting.SettingActivity"
            android:label="@string/setting" />
        <activity
            android:name=".demo.activity.DemoActivity"
            android:label="@string/app_name" >
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity android:name=".demo.activity.DemoFormWidgetActivity" />
        <activity android:name=".demo.activity.DemoContainerWidgetActivity" />
        <activity android:name=".demo.activity.DemoDialogActivity" />
        <activity
            android:name=".demo.activity.DemoThemeActivity"
            android:label="Theme Demo"
            android:configChanges="uiMode"/>
        <activity android:name=".ui.setting.SettingThemeActivity" />
    </application>

</manifest>